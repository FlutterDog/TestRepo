# LAI1118 Rev2 Firmware

Документация по встроенному программному обеспечению модуля аналоговых входов LAI.

## Назначение

Прошивка реализует прикладную логику модуля LAI:

- чтение восьми аналоговых входов через два преобразователя ADS1115;
- обмен по Modbus RTU через интерфейс RS-485;
- чтение аппаратного адресного переключателя;
- управление SPI-регистром индикации аналоговых входов;
- управление SPI-регистром режимов аналоговых входов;
- расчёт калиброванных значений аналоговых каналов;
- фиксацию максимальных значений по каналам;
- хранение параметров и калибровок в EEPROM;
- контроль потери связи с ведущим устройством;
- индикацию состояния связи;
- программную перезагрузку;
- переход в загрузчик для обновления прошивки.

Проект предназначен для микроконтроллера ATmega328P с тактовой частотой 16 MHz.

---

## Основные параметры

| Параметр | Значение |
|---|---|
| Микроконтроллер | ATmega328P |
| Частота CPU | 16 MHz |
| Интерфейс связи | RS-485 |
| Протокол | Modbus RTU |
| Скорость обмена | 9600 bit/s |
| Формат кадра | 8N1 |
| Количество аналоговых входов | 8 |
| АЦП | 2 × ADS1115 |
| Адреса ADS1115 | 0x49, 0x48 |
| Шина АЦП | I2C / TWI |
| Частота I2C | 400 kHz |
| Управление индикацией | SPI |
| Управление режимами входов | SPI |
| Системный тик | Timer0, 1 ms |
| Watchdog | используется |
| Bootloader jump | поддерживается |

---

## Состав проекта

```text
.
├── app/
│   ├── app.hpp
│   ├── app.cpp
│   ├── ads1115.hpp
│   ├── ads1115.cpp
│   ├── lai_board.hpp
│   ├── lai_board.cpp
│   └── project.hpp
│
├── hal/
│   ├── compat.hpp
│   ├── compat_gpio.cpp
│   ├── compat_serial.cpp
│   ├── compat_spi.cpp
│   ├── compat_tick.cpp
│   └── compat_twi.cpp
│
├── Libs/
│   ├── IGAS_mb.h
│   └── IGAS_mb.cpp
│
├── main.cpp
├── Doxyfile
└── README.md
```

### `main.cpp`

Точка входа прошивки.

Выполняет:

- запрет прерываний на этапе базовой инициализации;
- запуск системного миллисекундного тикера;
- разрешение прерываний;
- вызов `setup()`;
- бесконечный вызов `loop()`.

### `app/`

Прикладной уровень прошивки.

Содержит:

- инициализацию приложения;
- основной цикл;
- обработку Modbus-регистров;
- чтение аналоговых каналов;
- расчёт калиброванных значений;
- фиксацию максимальных значений;
- работу с EEPROM-параметрами;
- управление индикацией;
- управление режимами аналоговых входов;
- контроль таймаута связи;
- индикацию состояния связи;
- перезагрузку;
- переход в bootloader.

### `app/lai_board.hpp`, `app/lai_board.cpp`

Аппаратный слой платы LAI.

Содержит привязку сигналов платы к портам ATmega328P:

- адресный переключатель;
- RS-485 direction;
- SPI chip select для регистра индикации;
- SPI chip select для регистра режимов входов;
- разрешение сдвиговых регистров;
- светодиод состояния связи.

Прикладной код не должен напрямую работать с `PORTx`, `DDRx` и `PINx` для сигналов платы. Для этого используется `lai_board`.

### `app/ads1115.hpp`, `app/ads1115.cpp`

Драйвер аналого-цифрового преобразователя ADS1115.

Содержит:

- запуск одиночного преобразования;
- настройку диапазона входного усилителя;
- настройку режима работы;
- настройку скорости преобразования;
- проверку готовности результата;
- чтение шестнадцатиразрядного значения АЦП.

### `hal/`

Общий аппаратный слой.

Содержит:

- GPIO;
- UART0;
- SPI master;
- TWI/I2C master;
- системный тикер;
- задержки;
- минимальные системные типы и функции.

### `Libs/IGAS_mb.*`

Транспорт Modbus RTU по RS-485.

Поддерживаемые функции Modbus:

- FC03 — Read Holding Registers;
- FC06 — Write Single Register;
- FC16 — Write Multiple Registers.

Доступ к прикладной карте регистров выполняется через callback, назначаемый из `app.cpp`.

---

## Аппаратная карта LAI

| Сигнал | Линия MCU | Назначение |
|---|---|---|
| RXD | PD0 | Приём UART0 |
| TXD | PD1 | Передача UART0 |
| RS-485 DE / RE | PD2 | Управление направлением RS-485 |
| LED_SS | PD3 | Выбор SPI-регистра индикации |
| MODE_SS | PD4 | Выбор SPI-регистра режимов входов |
| SHIFT_EN | PD5 | Разрешение сдвиговых регистров |
| ADDR0 | PD6 | Адресный переключатель, bit 0 |
| ADDR1 | PD7 | Адресный переключатель, bit 1 |
| ADDR2 | PB0 | Адресный переключатель, bit 2 |
| ADDR3 | PB1 | Адресный переключатель, bit 3 |
| ADDR4 / SPI_SS | PB2 | Адресный переключатель, bit 4; линия SPI SS |
| SPI_MOSI | PB3 | SPI MOSI |
| SPI_MISO | PB4 | SPI MISO |
| SPI_SCK | PB5 | SPI SCK |
| STATUS_LED | PC0 | Светодиод состояния связи |
| I2C_SDA | PC4 | Линия данных I2C |
| I2C_SCL | PC5 | Линия тактирования I2C |

---

## Адресный переключатель

Адресный переключатель подключён к линиям `PD6`, `PD7`, `PB0`, `PB1`, `PB2`.

Соответствие входов и битов адреса:

| Линия MCU | Бит адреса | Маска |
|---|---:|---:|
| PD6 | bit 0 | 0x01 |
| PD7 | bit 1 | 0x02 |
| PB0 | bit 2 | 0x04 |
| PB1 | bit 3 | 0x08 |
| PB2 | bit 4 | 0x10 |

Вход с низким уровнем считается установленным битом адреса.

Если аппаратно считанный адрес равен нулю, прошивка использует адрес `1`.

Линия `PB2` используется на этапе старта как вход адресного переключателя. После чтения адреса и инициализации SPI эта линия переводится в выходной уровень HIGH для сохранения режима SPI master на ATmega328P.

---

## RS-485

Перед отправкой ответа Modbus трансивер переводится в режим передачи.

Возврат в режим приёма выполняется после события UART TX Complete, когда физическая передача последнего байта завершена.

Параметры обмена по умолчанию:

```text
9600 bit/s
8N1
Modbus RTU
```

---

## Аналоговые входы

Модуль использует два преобразователя ADS1115.

| Преобразователь | I2C-адрес | Каналы |
|---|---:|---|
| ADS_1 | 0x49 | четыре аналоговых входа |
| ADS_2 | 0x48 | четыре аналоговых входа |

Преобразователи работают в режиме одиночного преобразования.

Основные настройки:

| Параметр | Значение |
|---|---|
| Gain | 0 |
| Mode | single-shot |
| Data rate | 7 |
| I2C frequency | 400 kHz |

Чтение каналов выполняется циклически через автомат состояний:

```text
requestAI
delayADC
checkAIReady
getAIx
calcOutput
```

---

## Калибровка аналоговых каналов

Для каждого канала используются две калибровочные точки:

```text
sensorAnalogX[2n - 1], gasConcentrationY[2n - 1]
sensorAnalogX[2n],     gasConcentrationY[2n]
```

Калиброванное значение рассчитывается по линейной зависимости:

```text
y = k * (x - b)
```

где:

- `x` — код АЦП;
- `y` — калиброванное значение канала;
- `k` — коэффициент наклона;
- `b` — смещение.

Калибровочные параметры сохраняются в EEPROM командой через `HR 90`.

---

## Регистр индикации и регистр режимов входов

Индикация состояния аналоговых входов управляется через SPI-регистр `LED_SS`.

Режимы аналоговых входов управляются через SPI-регистр `MODE_SS`.

Параметр `AI_MODE` хранится в EEPROM по адресу `115` и передаётся в регистр режимов входов при старте прошивки.

Изменение `AI_MODE` через Modbus выполняется через `HR 115` после включения защищённого доступа через `HR 800`.

---

## EEPROM-карта

| Адрес EEPROM | Назначение |
|---:|---|
| 72 | Серийный номер |
| 100 | Modbus-адрес устройства |
| 110...113 | Порог фиксации максимального значения `MOTOR_STOP_THRESHOLD` |
| 115 | Режим аналоговых входов `AI_MODE` |
| 11...18 | Калибровочные параметры канала 1 |
| 19...26 | Калибровочные параметры канала 2 |
| 27...34 | Калибровочные параметры канала 3 |
| 35...42 | Калибровочные параметры канала 4 |
| 43...50 | Калибровочные параметры канала 5 |
| 51...58 | Калибровочные параметры канала 6 |
| 59...66 | Калибровочные параметры канала 7 |
| 67...74 | Калибровочные параметры канала 8 |

---

## Карта Modbus-регистров

| Holding Register | Доступ | Назначение |
|---:|---|---|
| 0 | Read | Битовая маска состояния аналоговых входов |
| 1...16 | Read | Калиброванные значения каналов `ADC_Converted` в формате float, два регистра на канал |
| 39 | Read | Серийный номер из EEPROM |
| 40 | Read | Версия встроенного ПО |
| 51...66 | Read | Максимальные значения каналов `maxCurrArray` в формате float, два регистра на канал |
| 90 | Write | Сохранение калибровок всех каналов в EEPROM |
| 100 | Read / Write | Modbus-адрес устройства |
| 101...102 | Read / Write | Порог фиксации максимального значения `MOTOR_STOP_THRESHOLD` в формате float |
| 115 | Read / Write | Режим аналоговых входов `AI_MODE` |
| 201...216 | Read | Текущие калибровочные значения `sensorAnalogX` |
| 301...316 | Read | Текущие калибровочные значения `gasConcentrationY` |
| 401...416 | Read / Write | Изменение `sensorAnalogX` |
| 501...516 | Read / Write | Изменение `gasConcentrationY` |
| 594 | Write | Программная перезагрузка |
| 600 | Read | Битовая маска состояния аналоговых входов |
| 601...608 | Read | Сырые значения `AIData` |
| 611...618 | Read | Состояния каналов `channelActive` |
| 621...636 | Read | Текущие максимальные значения активных импульсов `buffer` в формате float |
| 701...708 | Read | `ADC_Converted * 100` |
| 800 | Read / Write | Сервисный PIN-код |
| 900 | Read / Write | Отложенный переход в bootloader |
| 1000 | Read | Идентификатор устройства, слово 1 |
| 1001 | Read | Идентификатор устройства, слово 2 |
| 1100 | Read | Аппаратная ревизия, слово 1 |
| 1101 | Read | Аппаратная ревизия, слово 2 |

### HR 0 и HR 600 — битовая маска состояния аналоговых входов

Регистры возвращают битовую маску состояния аналоговых входов.

Маска формируется по результатам сравнения сырых значений каналов с внутренним порогом индикации.

### HR 1...16 — калиброванные значения аналоговых каналов

Регистры возвращают значения `ADC_Converted` в формате IEEE-754 float.

Каждое значение занимает два Holding-регистра.

### HR 39 — серийный номер

Регистр возвращает серийный номер из EEPROM.

### HR 40 — версия ПО

Регистр возвращает значение константы версии встроенного программного обеспечения.

### HR 51...66 — максимальные значения каналов

Регистры возвращают значения `maxCurrArray` в формате IEEE-754 float.

Каждое значение занимает два Holding-регистра.

### HR 90 — сохранение калибровок

Запись в `HR 90` сохраняет калибровочные точки всех восьми каналов в EEPROM.

### HR 100 — Modbus-адрес

При записи в `HR 100` значение младшего байта сохраняется как новый Modbus-адрес устройства.

Значение сохраняется в EEPROM и применяется к объекту Modbus RTU.

### HR 101...102 — порог фиксации максимального значения

Регистры содержат `MOTOR_STOP_THRESHOLD` в формате IEEE-754 float.

Запись разрешена после включения защищённого доступа через `HR 800`.

### HR 115 — режим аналоговых входов

Регистр содержит маску `AI_MODE`.

Запись разрешена после включения защищённого доступа через `HR 800`.

После записи значение сохраняется в EEPROM и передаётся в SPI-регистр режимов аналоговых входов.

### HR 401...416 и HR 501...516 — изменение калибровочных точек

Регистры используются для изменения калибровочных точек в оперативной памяти.

Для сохранения изменённых калибровок в EEPROM необходимо выполнить запись в `HR 90`.

### HR 594 — программная перезагрузка

Для перезагрузки записать:

```text
HR 594 = 1
```

После обработки команды прошивка выполняет перезагрузку через watchdog.

### HR 800 — сервисный PIN-код

Для включения защищённого доступа записать:

```text
HR 800 = 2210
```

При другом значении защищённый доступ отключается.

Защищённый доступ используется для записи `HR 101...102` и `HR 115`.

### HR 900 — переход в bootloader

Для перехода в загрузчик записать любое значение в `HR 900`.

Переход выполняется с небольшой задержкой, чтобы Modbus-ответ успел уйти в линию RS-485.

---

## Поведение при потере связи

Прошивка контролирует время последнего корректного Modbus-запроса.

Если в течение заданного таймаута обмен отсутствует:

- состояние связи считается потерянным;
- светодиод связи переходит в режим мигания.

При восстановлении обмена светодиод связи включается постоянно.

---

## Сборка прошивки

Проект собирается в Microchip Studio / Atmel Studio с AVR-GCC.

### Рекомендуемые параметры проекта

| Параметр | Значение |
|---|---|
| Device | ATmega328P |
| Toolchain | AVR-GCC |
| F_CPU | 16000000UL |
| Конфигурация сборки | Release или Debug |

### Порядок сборки

1. Открыть проект в Microchip Studio / Atmel Studio.
2. Проверить выбранный микроконтроллер: `ATmega328P`.
3. Проверить наличие определения `F_CPU = 16000000UL`.
4. Проверить наличие файлов `compat_spi.cpp` и `compat_twi.cpp` в составе проекта.
5. Выполнить сборку проекта.
6. Проверить отсутствие ошибок компиляции и линковки.
7. Получить файл прошивки `.hex` из выходной папки сборки.

Типовые выходные папки:

```text
Debug/
Release/
```

Пример ожидаемого результата:

```text
LAI1118_rev2.hex
```

Фактическое имя `.hex` зависит от имени проекта в среде разработки.

---

## Генерация Doxygen-документации

Doxygen-документация формируется из исходных файлов и комментариев в коде.

Рабочий каталог проекта:

```text
C:\Development\GccApplication1
```

Для генерации выполнить из корня проекта:

```bat
cd /d C:\Development\GccApplication1
if not exist docs\doxygen mkdir docs\doxygen
doxygen Doxyfile
```

Если требуется сохранить лог генерации:

```bat
doxygen Doxyfile > doxygen_build.log 2>&1
```

Результат генерации:

```text
docs\doxygen\html\index.html
```

Открыть документацию:

```bat
start docs\doxygen\html\index.html
```

---

## Прошивка микроконтроллера

Прошивка может загружаться одним из двух способов:

- через ISP-программатор;
- через установленный bootloader, если он записан в микроконтроллер и настроен корректно.

### Прошивка через ISP

Общий порядок:

1. Подключить питание модуля.
2. Подключить ISP-программатор к разъёму программирования.
3. Выбрать микроконтроллер `ATmega328P`.
4. Выбрать файл `.hex`.
5. Выполнить запись flash.
6. Перезапустить модуль.
7. Проверить связь по Modbus RTU.
8. Проверить чтение аналоговых каналов.

### Прошивка через bootloader

Общий порядок:

1. Подключить RS-485 интерфейс.
2. Перевести модуль в bootloader.
3. Загрузить файл `.hex` через используемый загрузчик.
4. Перезапустить модуль.
5. Проверить связь по Modbus RTU.
6. Проверить чтение аналоговых каналов.

Переход в bootloader из рабочей прошивки выполняется записью в `HR 900`.

---

## Проверка после прошивки

После загрузки прошивки проверить:

1. Модуль отвечает по Modbus RTU.
2. Адрес соответствует положению аппаратного адресного переключателя.
3. `HR 0` или `HR 600` возвращает битовую маску состояния аналоговых входов.
4. `HR 1...16` возвращают калиброванные значения каналов.
5. `HR 601...608` возвращают сырые значения каналов.
6. `HR 40` возвращает версию ПО.
7. `HR 100` возвращает текущий Modbus-адрес.
8. При отсутствии обмена светодиод связи переходит в режим мигания.
9. При восстановлении обмена светодиод связи включается постоянно.
10. Запись в `HR 594 = 1` выполняет программную перезагрузку.
11. Переход в bootloader через `HR 900` проверяется только при наличии корректного загрузчика.

---

## Минимальный тест Modbus

Параметры порта:

```text
9600 bit/s
8N1
Modbus RTU
```

Базовые регистры для проверки:

| Проверка | Регистр |
|---|---:|
| Битовая маска входов | HR 0 или HR 600 |
| Калиброванные значения | HR 1...16 |
| Версия ПО | HR 40 |
| Максимальные значения | HR 51...66 |
| Modbus-адрес | HR 100 |
| Сырые значения АЦП | HR 601...608 |
| Сервисный PIN-код | HR 800 |
| Перезагрузка | HR 594 |
| Переход в bootloader | HR 900 |

Пример проверки:

```text
Read HR 40
Read HR 100
Read HR 600
Read HR 601...608
Read HR 1...16
Write HR 800 = 2210
Read HR 800
```

---

## Возможные ошибки

| Признак | Возможная причина | Проверка |
|---|---|---|
| Нет ответа по Modbus | Неверный адрес | Проверить адресный переключатель |
| Нет ответа по Modbus | Неверные параметры UART | Проверить 9600 bit/s, 8N1 |
| Нет ответа по Modbus | Ошибка RS-485 A/B | Проверить полярность линии |
| Ответы нестабильны | Ошибка управления DE/RE | Проверить линию RS-485 direction |
| Нет значений АЦП | Ошибка I2C | Проверить линии SDA/SCL и адреса ADS1115 |
| Нет значений АЦП | Не добавлен `compat_twi.cpp` в проект | Проверить состав файлов проекта |
| Не работает индикация | Ошибка SPI | Проверить MOSI/SCK/SS и `compat_spi.cpp` |
| Не меняется режим входов | Не включён защищённый доступ | Записать `HR 800 = 2210` |
| Не сохраняется калибровка | Не выполнена запись в `HR 90` | Проверить последовательность записи калибровок |
| Адрес не совпадает с переключателем | Ошибка порядка битов адресного переключателя | Проверить PD6, PD7, PB0, PB1, PB2 |
| Прошивка перезагружается | Watchdog не сбрасывается | Проверить зависание основного цикла |
| Не работает переход в bootloader | Несовпадение bootloader address / fuse | Проверить BOOTSZ и адрес boot-секции |

---

## Примечания по сопровождению

Проект передаётся как исходный код встроенного ПО с базовой документацией в комментариях Doxygen.

Перед внесением изменений рекомендуется:

1. Сначала собрать проект без изменений.
2. Сохранить рабочий `.hex`.
3. Проверить связь по Modbus RTU на стенде.
4. Проверить чтение всех аналоговых каналов.
5. Проверить запись и чтение основных Modbus-регистров.
6. После изменений повторить проверку регистров, аналоговых каналов, SPI-регистров и EEPROM-параметров.
7. Проверить переход в bootloader только при наличии корректного загрузчика.

Изменения аппаратной карты платы следует выполнять в файлах:

```text
app/lai_board.hpp
app/lai_board.cpp
```

Изменения драйвера ADS1115 следует выполнять в файлах:

```text
app/ads1115.hpp
app/ads1115.cpp
```

Изменения прикладной карты Modbus следует выполнять в файле:

```text
app/app.cpp
```

Изменения транспорта Modbus RTU следует выполнять только при необходимости и после проверки на стенде:

```text
Libs/IGAS_mb.h
Libs/IGAS_mb.cpp
```

---

## Состав передаваемых материалов

Рекомендуемый минимальный комплект для передачи:

```text
исходный код проекта;
Doxyfile;
README.md;
сгенерированная Doxygen-документация;
скомпилированный HEX-файл;
краткая инструкция по прошивке;
краткая инструкция по проверке Modbus-регистров.
```

Этот комплект достаточен для сборки, просмотра структуры проекта, генерации документации, загрузки прошивки и базовой проверки работоспособности модуля.
