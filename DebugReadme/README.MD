





## 4. Описание электрической схемы по функциональным узлам

**Легенда пометок (в тексте ниже):**  
- 🔵 *Информация / пояснение*  
- 🟢 *Зафиксировано “as-built” (по схеме/BOM)*  
- 🟡⚠️ *Внимание / важно учесть / нужно уточнение*  
- 🔴 *Ограничение / не гарантируется / опасный режим*

---

## 4.1 Вход питания и защиты (полярность, предохранение, TVS, фильтры, режимы отказа)

🔵 Источники: **Plugs.SchDoc** (ввод разъёмом), **Power Supply.SchDoc** (цепи защиты и DC/DC).

---

### 4.1.1 Ввод питания на плату (разъём, сети, “земля/шасси”)

🟢 **Ввод питания выполнен по сети `+24V` и `GND`** (локальная “0V” платы).  
🟢 **Подключения к PE/корпусной земле на плате не предусмотрено** (корпус пластик; плата электрически “плавающая” относительно PE).

🟢 Разъём ввода/связи (по BOM):  
- **P3 (X2X)** — Phoenix/класс клеммника 5 pin (BOM: `EMSTBA 2.5/5-G-5.08`, Comment: `X2X`).

🟡⚠️ По вашей вводной: на X2X совмещены **питание 24V (2 пина)** + **RS-485 A/B (2 пина)** + **средний пин NC (ранее “земля”, но не подключён)**.  
> Это нужно зафиксировать в ПЗ как *распиновку разъёма*, но в схеме (PDF) векторно это читается не везде — лучше подтвердить по Altium/Pinout-таблице.

---

### 4.1.2 Первичная защита входа (переполюсовка, перегрузка)

🟢 На входе `+24V` реализованы следующие функции защиты (**Power Supply.SchDoc**):

1) **Самовосстанавливающийся предохранитель (PTC)**  
- **F2** — PTC resettable fuse (по надписи на схеме: `MF-MSMF030-2 0.50A`; в BOM позиция есть как `F2`, но без уточнённого Comment/MPN).  
🔵 Назначение: ограничение тока при перегрузке/аварии; при устранении причины восстанавливается.

2) **Защита от обратной полярности**  
- **D2** — диод последовательно во входной цепи (Schottky).  
  - по схеме: `B360A-13-F`  
  - по BOM: `B340A-13-F`  
🟡⚠️ Нужна финальная фиксация “as-built”: **какой именно MPN установлен** (B340A vs B360A). В ПЗ можно написать нейтрально: *Schottky SMA, установлен по BOM*.

🔵 В результате: после F2 + D2 формируется узел входного питания для DC/DC (на схеме помечен как **`V_div`**, см. ниже).

---

### 4.1.3 TVS/ограничение перенапряжений (как реализовано в этой ревизии)

🟢 **TVS на входе `+24V` отсутствует** (по вашей правке: *«TVS на входе нет»*).  
🔵 То есть входная защита в основном: **PTC + последовательный диод**.

🟢 При этом **TVS установлен на шине `+5V`**:  
- **TVS1** — Comment: `SMAJ5.0CA` (BOM); стоит между `+5V` и `GND` (**Power Supply.SchDoc**).  
🔵 Назначение: ограничение выбросов уже на вторичном питании `+5V` (локальные импульсы/ESD/наводки внутри платы).

🔴 Режим “подача 220V на вход 24V” не нормируется и не рассматривается как допустимый режим: последствия непредсказуемы (ограничение по компонентам входной цепи и трассировке).

---

### 4.1.4 Узел `V_div` (что это такое по факту)

🟢 В **Power Supply.SchDoc** узел после D2 обозначен как **`V_div`** и одновременно используется как **входное питание `Vin` для U2 (buck-конвертер)**.

🟡⚠️ В текущем PDF-экспорте схемы **не видно отдельного делителя**, который бы делал “div” (деление) для измерения АЦП — поэтому по факту сейчас это выглядит как *имя узла*, а не “делитель”.  

🟡⚠️ Уточнение нужно, чтобы корректно описать без домыслов:
- `V_div` — это просто “VIN после защиты” (имя историческое)?  
- или это действительно сигнал измерения входного питания (и тогда: **где делитель/на какой пин АЦП приходит/какой коэффициент?**)  

---

### 🟡⚠️ Вопросы/уточнения по 4.1 (коротко)

1) Подтвердить **распиновку P3/X2X** (какие номера пинов: +24V, GND, A, B, NC).  
2) Подтвердить **установленный MPN D1/D2** (B340A-13-F или B360A-13-F).  
3) Уточнить назначение **`V_div`** (только Vin или ещё и измерение).

---

## 4.2 Стабилизаторы и опорные напряжения (номиналы, токи, причины выбора)

🔵 Источники: **Power Supply.SchDoc**, частично **Processor.SchDoc** (фильтрация доменов питания).

---

### 4.2.1 Общая карта напряжений (что откуда берётся)

🟢 На плате выделены основные шины питания:

- **`+24V` (вход)** → первичный ввод питания на плату (см. 4.1).  
- **`+5V`** — формируется импульсным buck-конвертером **U2**.  
  🔵 Использование (по вашей вводной): питание **RS-485 драйверов** и **индикации (LED)**.
- **`+3V3`** — формируется линейным стабилизатором **U3** от `+5V`.  
  🔵 Использование (по вашей вводной): питание **ATSAM3X8E**, **W5500** и остальной логики 3.3V.

🟡⚠️ Токи потребления по шинам в схеме не заданы — в ПЗ их обычно фиксируют как “расчётные/типовые” (по факту измерений или по спецификациям). Если хотите — добавим таблицу “Rail → I_typ/I_max”.

---

### 4.2.2 Buck-конвертер 24V → 5V (U2 TPS54331)

🟢 Преобразователь:
- **U2 = TPS54331** (BOM: `TPS54331`, корпус SOIC-8).  
- Топология: классический **step-down (buck)** с внешним дросселем и диодом.

🟢 Силовая часть:
- **L5** — дроссель (BOM: серия `CDRH127/...`; Comment содержит `...-220...` → требуется подтверждение номинала, см. ниже).  
- **D1** — диод свободного хода (Schottky; аналогично D2: по схеме `B360A-13-F`, по BOM `B340A-13-F`).  
- **C2/C9/C10** — выходные ёмкости **22uF + 22uF + 22uF** (итого ~66uF), корпус 1206.

🟢 Формирование выходного напряжения `+5V` (обратная связь):  
- Делитель собран **цепочкой резисторов**: **R8 (10k), R7 (510), R75 (1k), R10 (1k)**.  
🔵 Такая компоновка даёт эквивалент делителя для установки Vout около 5V (по типовой формуле для TPS54331 с Vref≈0.8V).

🟢 Порог включения (EN / UVLO):  
- **R9 (100k) + R12 (10k)** образуют делитель на вход **EN**.  
🔵 Оценочно это задаёт включение преобразователя при Vin порядка **~14V** (уровень зависит от порога EN конкретной микросхемы).  
🟡⚠️ Если планируется эксплуатация при пониженном входе (ниже ~14V), это важно отдельно отметить.

🟢 Компенсация/soft-start/boot (как реализовано):
- **C23 0.1u** — bootstrap (BOOT–PH).  
- **C28 6.8n** — soft-start (SS–GND).  
- **R11 10k, C22 6.8n, C27 18pF** — цепь COMP (компенсация) (как “as-built”).

🔴 EMI/входной фильтр:  
- На входе **нет отдельного LC-фильтра и входного TVS по `+24V`** (см. 4.1).  
- В этой ревизии стабилизация и помехоустойчивость опираются на базовую обвязку U2 + входные/выходные конденсаторы.

---

### 4.2.3 LDO 5V → 3.3V (U3, шина +3V3)

🟢 Линейный стабилизатор:
- **U3** — серия 1117 в корпусе **SOT-223**.  
  - по схеме подписан как **LM1117…-3.3**  
  - по BOM указан **NCV1117…** (в LibRef встречается `...ST50...`, что выглядит как 5.0V-вариант — нужно уточнить “as-built”, см. вопросы).  
🟢 Выходная сеть: **`+3V3`**.

🟢 Конденсаторы (как реализовано):
- **C32 0.1u** — входной (по схеме у U3).  
- **C24 22uF** — выходной (по схеме у U3).  
🔵 Номиналы и тип (керамика) зафиксированы “as-built”; подбор выполнен по референсной схеме/даташиту используемого LDO семейства 1117.

---

### 4.2.4 Локальная фильтрация доменов питания (ферриты, AVREF/PLL)

🟢 Для разделения “шумных/чувствительных” доменов применены ферритовые бусины (по **Processor.SchDoc**):
- **L1–L4** — Comment: `100MHz`, LibRef: `BLM21PG220SN1D` (0805).  
🔵 Используются для питания доменов MCU типа **VDDANA / AVREF / VDDPLL** и т.п. (с локальными конденсаторами рядом с выводами).

🟢 Примеры локальных ёмкостей (по BOM, встречаются на Processor.SchDoc):
- **C13/C14/C16/C11/C12** — 0.1u (0603)  
- **C59** — 22uF (1206)

---

### 🟡⚠️ Вопросы/уточнения по 4.2

1) Подтвердить **номинал дросселя L5** (по BOM видна серия CDRH127, но “220/270” в Comment/Footprint расходится).  
2) Подтвердить “as-built” по **U3**: фактический MPN (3.3V вариант 1117), чтобы не было конфликта BOM vs подпись на схеме.  
3) Хотите ли вы добавить в ПЗ **минимальную таблицу по питанию**: `Rail → I_typ / I_max / точки измерения` (без графиков), а графики/отчёты — вынести в Приложение (если они у вас есть отдельным файлом)?

---

## 4.3 МК/SoC и обвязка (тактирование, reset, boot straps, brown-out)

🔵 Источник: **Processor.SchDoc** (основной), частично **Plugs.SchDoc** (USB-разъём).

---

### 4.3.1 Микроконтроллер и питание доменов

🟢 Основной МК:
- **U1 = ATSAM3X8EA-AU** (BOM), корпус **LFBGA-144**.

🟢 Питание МК:
- Внешняя шина питания МК: **`+3V3`**.  
- Для “чувствительных” доменов применены **ферритовые бусины L1–L4 (BLM21PG220SN1D)** и локальная развязка конденсаторами (см. 4.2.4).  
🔵 Такое разделение используется для снижения влияния цифровых переключений на аналоговые/PLL-цепи.

🔵 Внутренние функции brown-out / POR относятся к настройкам МК и фиксируются в документации прошивки.  
🟢 На уровне “железа” отдельного внешнего супервизора питания (reset supervisor) в этой ревизии не предусмотрено (по схеме).

---

### 4.3.2 Тактирование (основной генератор + RTC)

🟢 Основной кварц:
- **XTL6 = 12.0 MHz** (BOM)  
- Конденсаторы: **C17 = 18pF, C18 = 18pF**

🟢 RTC-кварц (низкочастотный):
- **XT1 = 32.768 kHz** (BOM)  
- Конденсаторы: **C19 = 18pF, C20 = 18pF**

🔵 Номиналы кварцев/ёмкостей зафиксированы “as-built”; согласование по нагрузочной ёмкости кварца — по даташиту кварцев и рекомендациям MCU.

---

### 4.3.3 Reset (MASTER-RESET)

🟢 Аппаратный reset выполнен через линию **`MASTER-RESET`**:
- **Btn1** — тактовая кнопка (Reset).  
- **C26 = 6.8n** — конденсатор в цепи reset (по схеме).  
🔵 Подробная логика “кто тянет линию вверх/вниз” определяется внутренними подтяжками МК и конкретной обвязкой на схеме (в этой ревизии реализовано согласно Processor.SchDoc).

---

### 4.3.4 Питание RTC от батарейки (backup supply)

🟢 Для сохранения хода RTC при отсутствии `+3V3` установлен держатель батарейки:
- **B1** — Battery Holder **CR2032** (BOM).

🟢 Развязка питания RTC/backup домена:
- **D4 = BAT54C** — диодная сборка (SOT-23), используется для “диодного OR” между:
  - **`+3V3`** (когда основное питание есть)
  - **VBAT (CR2032)** (когда основное питание отсутствует)
🔵 Назначение: питание backup-домена МК (VDDBU/аналогичный) без обратного тока в батарейку.

---

### 4.3.5 Контроль батарейки (наличие/уровень)

🟢 На схеме присутствует узел контроля батарейки:
- **U21 = TS391ILT** (SOT-23-5)  
- **R2/R102/R103 = 10k** (делители/опорные цепи)  
🔵 Выход узла заведён на вход МК (на схеме встречается метка **PIN50**).

🟡⚠️ Без уточнения порога/логики (и без привязки к конкретному пину/каналу) в ПЗ лучше описывать это нейтрально как:
> “аппаратный узел контроля состояния батареи/backup питания, сигнал подаётся на МК”.

---

### 4.3.6 USB-линии (как часть обвязки МК)

🟢 На линии USB предусмотрены элементы согласования/защиты:
- **R3 = 39Ω, R4 = 39Ω** — последовательные резисторы в D+/D−.  
- **Z1/Z2 = CG0603MLC-05E** — ESD/варисторы (0603) на линиях USB.

🔵 Разъём USB физически вынесен на отдельный лист (Plugs.SchDoc), но электрически относится к обвязке МК.

---

### 🟡⚠️ Вопросы/уточнения по 4.3

1) Подтвердить: **какие именно пины/сигналы используются как “boot straps / erase / recovery”** в этой ревизии (если они вообще выведены/реализованы в железе).  
2) Подтвердить “as-built” назначение **U21 (TS391)**: это “battery low”, “battery present” или иной порог/логика?  
3) Хотите ли вы в ПЗ фиксировать **таблично**: `Кварц → нагрузочная ёмкость/ESR (по даташиту)` или оставляем только номиналы (как сейчас)?

---

## Следующий пункт по оглавлению

### 4.4 Память и хранение конфигурации (Flash/FRAM/EEPROM/SD)

🟡⚠️ Для 4.4 мне нужно (я постараюсь взять из схемы/BOM, но лучше подтвердить):
1) Какие именно “памяти” физически есть на плате помимо внутренней Flash МК: **microSD-держатель (есть), внешняя EEPROM/FRAM (есть/нет)**.  
2) Если есть SD: тип разъёма/линии интерфейса (SPI/SDIO), уровни (3.3V), наличие ESD/серийных резисторов (уточню по схеме).  
3) Если на плате есть отдельные микросхемы памяти — подтвердить “as-built” (RefDes/MPN) или я подниму их из BOM.



---

## 4.4 Память и хранение конфигурации (Flash/EEPROM/FRAM/SD) — as-built

🟦ℹ️ Цель раздела: зафиксировать аппаратные носители данных и интерфейсы хранения, которые реально присутствуют на плате.

### 4.4.1 Внутренняя память МК (Flash/ROM/RAM)

🟦ℹ️ На плате установлен МК **ATSAM3X8EA-AU (U1)**.  
🟦ℹ️ Внутренние ресурсы (Flash/RAM/ROM) являются частью МК и отдельными элементами на схеме не выделяются.

**RefDes/MPN (по BOM):**  
- U1: `ATSAM3X8EA-AU`, корпус `LFBGA-144`

🟨⚠️ Вопрос (чтобы корректно описать “границу” раздела):  
Нужно ли в ПЗ на плату фиксировать **только наличие МК** (рекомендуется), или вы хотите добавить 1 строку “объём внутренней Flash/ОЗУ” (это уже справка из даташита)?

---

### 4.4.2 microSD (внешний носитель на плате)

🟦ℹ️ На плате установлен **держатель microSD** и реализован интерфейс SD-карты на уровне схемы.

**RefDes/MPN (по BOM):**  
- SD1: `DM3AT-SF-PEJM5` (держатель microSD)

**Аппаратные сигналы (по схеме):**  
- `SDSPCK` (SCK)  
- `CMD /DI` (MOSI/DI)  
- `DATA0 /DO` (MISO/DO)  
- `DATA3-CD/CS` (CS + card detect в зависимости от режима)  
- `SD_Detect` (выделенный сигнал детекта карты)  
- питание: `+3V3`  
- земля: `GND`

🟦ℹ️ По текущей реализации microSD запитана от **+3V3** (общий домен логики).

🟨⚠️ Вопросы (если нужно сделать описание точным без домыслов):  
1) Подтвердить режим интерфейса SD1: **SPI** (по именам линий похоже на SPI), либо у вас используется SDIO-режим?  
2) Есть ли на линиях SD **серийные резисторы/ESD-защита** (в PDF это есть, но нужно уточнить RefDes на листе SD).

---

## 4.5 Интерфейсы связи (физический уровень на плате)

🟦ℹ️ В этом разделе описывается только “железо”: трансиверы/разъёмы/защита/уровни.  
🟨⚠️ Не описываем: протоколы как требования, режимы ПО, “сколько сокетов” и т.п.

---

### 4.5.1 RS-485 (физический уровень)

🟦ℹ️ На плате реализовано **несколько каналов RS-485** на дискретных трансиверах.

**RefDes/MPN (по BOM):**  
- U4–U10: `MAX485` (7 шт., footprint “MAX485 - duplicate”)

🟦ℹ️ Питание RS-485 части: **+5V** (по вашей фиксированной вводной: “5V идёт на питание 485 драйверов и индикации”).  
🟦ℹ️ Управление направлением (DE/RE) и линии UART приходят от МК (U1) напрямую или через расширитель UART (см. ниже U18).

🟦ℹ️ Защита линий RS-485: на схеме присутствует группа TVS-диодов для линий/портов (см. лист с интерфейсами, где видны `TVS16…TVS22` и т.п.).

**RefDes TVS (по BOM):**  
- TVS16–TVS22: “TVS Diode” (DO-214AC), без конкретного MPN в Comment (нужно уточнить тип по схеме/описанию, если критично).

🟨⚠️ Вопросы для закрытия 4.5.1:  
1) Подтвердить: **какие порты RS-485 физически выведены на разъёмы**, и какие именно разъёмы/клеммники это делают (кроме X2X).  
2) Подтвердить: есть ли на портах **оконечивание (120Ω)** на плате или оно только внешнее/перемычками.  
3) Нужны RefDes цепей: **DE/RE**, **RO/DI**, и какие UART-линии к каким MAX485 привязаны (чтобы не гадать по выдернутому тексту PDF).

---

### 4.5.2 Ethernet (физический уровень)

🟦ℹ️ На плате реализованы **два независимых Ethernet-канала** на аппаратных контроллерах.

**RefDes/MPN (по BOM):**  
- U11: `W5500`, `LQFP-48`  
- U16: `W5500`, `LQFP-48`  
- ETH1, ETH2: `J0011D21BNL` (RJ45, footprint `RJ45`)

🟦ℹ️ Связь контроллеров Ethernet с МК выполнена по **SPI** (по схеме присутствуют линии `MOSI/MISO/SCLK`, отдельные `CS`, а также сигналы `INTn` и `RSTn` для каждого канала — в PDF встречается `RSTn_Eth2`, `INTN_2`).

🟦ℹ️ Питание Ethernet-части: домен **+3V3** (W5500 питается от 3.3V).

🟨⚠️ Вопросы для закрытия 4.5.2:  
1) Подтвердить: оба W5500 реально “as-built” (установлены) и оба RJ45 активны в изделии.  
2) Уточнить по схеме RefDes линий `CS/INT/RST` для U11 и U16 (чтобы в тексте это было 1:1 со схемой).  
3) Уточнить: есть ли отдельная ESD-защита на линиях Ethernet (на разъёме/перед трансформатором), и её RefDes.

---

### 4.5.3 UART-расширитель (для дополнительных последовательных каналов)

🟦ℹ️ Для расширения количества UART-каналов на плате установлен специализированный мост/контроллер.

**RefDes/MPN (по BOM):**  
- U18: `SC16IS752` (TSSOP-28)

🟦ℹ️ По схеме присутствуют сигналы `SCLK`, `SPI`, `IRQ`, `CS_...` вокруг U18 — то есть U18 подключён к МК по сериализованному интерфейсу (SPI/I2C в зависимости от конфигурации, в PDF читается как SPI-обвязка).

🟨⚠️ Вопросы для закрытия 4.5.3:  
1) Подтвердить: U18 подключён к МК по **SPI** или по **I2C** (в тексте нужно указать точно).  
2) Подтвердить: какие именно UART-каналы U18 далее уходят на какие MAX485/разъёмы.

---

## 4.6 Дискретные/аналоговые цепи (если присутствуют на плате)

🟦ℹ️ Раздел заполняется только тем, что реально разведено/присутствует на плате (не про “возможности МК”).

🟦ℹ️ По BOM видны элементы, которые могут относиться к дискретным цепям:
- U12–U15, U17: элементы с LibRef `Gate` (SOT-23-5) — вероятно логические ключи/буферы/управляющие элементы в дискретных цепях.
- Btn1/Btn2: тактовые кнопки (см. 4.7/4.3 reset).

**RefDes/MPN (по BOM):**  
- U12, U13, U14, U15, U17: `Gate` (SOT23-5)  
- Btn1, Btn2: `Tact Switch`

🟨⚠️ Вопросы для закрытия 4.6 (без домыслов):  
1) Нужно подтвердить по схеме: что именно коммутируют U12–U15/U17 (LED? линии управления? дискретные выходы?)  
2) Есть ли на плате реальные **аналоговые входы/цепи измерения** (кроме контроля батареи) и куда они выведены (разъём/пины).

---

## 4.7 Индикация и органы управления (LED, кнопки)

🟦ℹ️ На плате предусмотрены элементы индикации и кнопки обслуживания.

**RefDes (по BOM):**  
- LED1–LED5: позиции “Light Guide 2 / LED SMD DayLight” (в BOM есть неоднозначность: часть выглядит как световоды, часть как реальный LED).  
- Btn1, Btn2: тактовые кнопки.

🟦ℹ️ По PDF-сборке платы (видно на сводной странице) присутствуют как минимум **LED5/LED6** (нужно выровнять с BOM — в BOM LED6 может быть в другом виде обозначения).

🟨⚠️ Вопросы для закрытия 4.7:  
1) Подтвердить: какие из LED1–LED5 — **реальные светодиоды**, а какие — **световоды/оптика** (по BOM сейчас смешано).  
2) Уточнить: есть ли LED6 как отдельная позиция (в BOM неочевидно) и где она в схеме.  
3) Нужно ли фиксировать “назначение индикации” (например “POWER/ETH/SD”) — это можно оставить 1 строкой, но без логики прошивки.

---

## 4.8 Отладка и программирование (JTAG/USB/Reset)

🟦ℹ️ Раздел фиксирует аппаратные сервисные интерфейсы, доступные на плате.

### 4.8.1 JTAG (программирование/восстановление)

🟦ℹ️ На плате предусмотрен разъём/гребёнка для JTAG.

**RefDes/MPN (по BOM):**  
- P1: `Header 6X2` (footprint `mcd 1,5/6-G1-3.81`)

🟦ℹ️ На листе Processor.SchDoc присутствуют JTAG-сигналы `TCK/TMS/TDI/TDO` и `NRST` (в PDF они извлекаются на странице процессора).

🟨⚠️ Вопросы для закрытия 4.8.1:  
1) Подтвердить pinout P1: какие сигналы на каких пинах (минимально: TCK/TMS/TDI/TDO/NRST/GND/VTref).  
2) Подтвердить: предусмотрено ли “keying”/ориентация разъёма (метка 1-го пина на silk).

---

### 4.8.2 USB (сервисный интерфейс)

🟦ℹ️ На плате установлен разъём USB типа B.

**RefDes/MPN (по BOM):**  
- J1: `USB B` (LibRef `USBB-1J`)

🟦ℹ️ На линиях USB предусмотрены:
- серийные резисторы (в BOM: R3=39Ω, R4=39Ω),
- ESD-защита (в BOM: Z1/Z2 `CG0603MLC-05E`).

---

### 4.8.3 Reset (кнопка/цепь)

🟦ℹ️ На плате предусмотрена аппаратная линия сброса и кнопка Reset:
- Btn1 (как минимум одна кнопка участвует в reset-цепи),
- на схеме присутствует линия `MASTER-RESET` и конденсатор в цепи (например C26=6.8n).

🟨⚠️ Вопросы для закрытия 4.8.3:  
1) Подтвердить: Btn2 — это отдельная сервисная кнопка (не reset)? какое назначение на схеме.  
2) Уточнить RefDes подтяжки reset-линии (R?) чтобы описать её корректно.

---

# Следующий пункт по оглавлению

## 5. Описание печатной платы (PCB)
(габариты/стек/трассировка/зонирование/DFM/DFT/производственные файлы)

🟨⚠️ Чтобы раздел 5 получился фактическим (а не общими словами), кроме схем и BOM понадобятся:
- габарит/крепёж (DXF/Mechanical layer или скрин с размером),
- stackup (у вас уже есть скрин Layer Stack Manager — пригодится),
- assembly/PCB top/bot картинки (для зонирования и DFT).


---

# 5. Описание печатной платы (PCB) — fact/BOM-check

ℹ️ В этом разделе фиксируем **только PCB/производственные аспекты**: механика, стек, правила трассировки, зонирование, DFM/DFT, состав выходных файлов.  
⚠️ По текущим загруженным данным у меня **нет самого PCB-файла/герберов/assembly-выводов**, поэтому часть пунктов будет “что вижу / что нужно догрузить”.

---

## 5.1 Габариты, посадочные, крепёж, разъёмы (механика, ограничения корпуса)

ℹ️ **Что могу зафиксировать по текущим файлам:**
- По BOM видно, что на плате присутствуют “крупные” механически значимые элементы:
  - ETH1/ETH2: RJ45 `J0011D21BNL`
  - SD1: microSD holder `DM3AT-SF-PEJM5`
  - P3 (X2X): клеммник `EMSTBA 2.5/5-G-5.08`
  - J1: USB-B `USBB-1J`
  - P1: Header 6x2 (JTAG)
  - B1: holder CR2032

⚠️ **Чего не вижу в текущих данных (нужно, чтобы 5.1 стал “закрытым”):**
1) Габарит платы (L×W), координаты/диаметр крепёжных отверстий, keepout-зоны.  
2) Ограничения корпуса (высота над платой, доступ к microSD/USB/RJ45).  
3) Механические слои/чертёж платы (DXF/PDF “PCB drawing” или скрин с размерами из Altium).

**Минимум, что нужно догрузить для 5.1:**
- Скрин/экспорт из Altium: *Board Shape + Dimensions* и *Hole Table* (или PDF “PCB 2D Print” с размерами).

---

## 5.2 Стек слоёв (кол-во слоёв, толщины, медь, диэлектрики — as-built)

ℹ️ Источник: ваш скрин **Layer Stack Manager** (image (8).png).  
✅ По скрину видно:

- **Общая толщина платы:** ~**1.50032 mm** (Total Thickness).
- **Медь (сигнальные и плоскости):** 0.018 mm (≈ 18 µm) на слоях Top/GND/Power/HighSpeed/Bottom (по таблице).
- **Диэлектрик:** FR-4, εr ~ **4.2** (по таблице).
- **Солдермаска:** ~0.01016 mm (Solder Resist), εr ~ **3.5** (по таблице).

⚠️ По таблице видно несколько внутренних слоёв с названиями `Gnd`, `Power`, `HighSpeed/High speed`, что соответствует **многослойной плате (ориентировочно 6+ слоёв)**, но без исходного .PcbDoc я не могу корректно зафиксировать:
- точное количество **сигнальных** и **плоскостных** слоёв,
- назначение каждого слоя (какая плоскость что несёт),
- правила “Layer Pairs”, blind/buried via (если есть).

**Что нужно догрузить, чтобы 5.2 стал точным:**
1) Экспорт **Stackup** из Altium (CSV/PDF) или сам .PcbDoc.  
2) Если есть — таблица **via types** (сквозные/слепые) и минимальные допуски производителя.

---

## 5.3 Критичные цепи и правила трассировки

### 5.3.1 Обязательные правила (инварианты)

ℹ️ Без PCB-файла я фиксирую только “инварианты”, которые следуют из схемы:

- **Петля DC/DC (U2 TPS54331):**  
  ⚠️ минимизировать площадь высокочастотной петли (SW/PH ↔ D1 ↔ Cout ↔ GND).  
  ⚠️ выходные конденсаторы +5V (C2/C9/C10) — максимально близко к силовой части.

- **Развязка доменов питания MCU (ферриты L1–L4):**  
  ⚠️ феррит + локальные C должны быть максимально близко к соответствующим пинам питания/AVREF/PLL.

- **RTC-кварц 32.768 kHz (XT1) и 12 MHz (XTL6):**  
  ⚠️ короткие симметричные линии, “тихая” земля, удаление от SW/DC/DC.

- **SPI-шины (W5500, microSD, SC16IS752):**  
  ⚠️ ограничить длины и помехи, избегать прохождения под/рядом с SW-узлом buck, сохранить целостность возвратных токов.

- **Ethernet/RJ45 области:**  
  ⚠️ соблюдение рекомендаций по размещению разъёма и “чистой” зоне вокруг него (особенно если есть ESD/TVS элементы на линии).

### 5.3.2 Рекомендуемые правила (качество/запас)

ℹ️ Эти правила улучшают запас по стабильности, но не являются обязательными “по факту схемы”:

- согласование длин на SPI там, где частоты высокие/критичные (по факту измерений/скоростей),
- отделение “шумной” зоны DC/DC от кварцев и аналоговых доменов,
- если есть полигоны питания: достаточная ширина/толщина для токов +5V, отсутствие “узких горлышек”.

⚠️ Вопрос: есть ли у вас зафиксированные правила PCB (Design Rules) из Altium? Если да — можно экспортировать и приложить как “as-built”.

---

## 5.4 Размещение компонентов и зонирование (тепло, noisy/sensitive зоны, вводы/ESD-зоны)

ℹ️ По схеме можно определить ожидаемое зонирование, но для подтверждения нужен вид PCB (Top/Bottom).

**Ожидаемые зоны (по функционалу):**
- **Noisy zone:** DC/DC U2 + L5 + D1 + выходные C (узел +5V).  
- **Sensitive zone:** кварцы XTL6/XT1 и обвязка, домены питания MCU через L1–L4.  
- **Connector/ESD zone:** RJ45 ETH1/ETH2, USB, microSD, клеммник X2X.

⚠️ Чтобы описать 5.4 без домыслов, нужно:
- 1–2 картинки (assembly/top view) с отмеченными зонами,
- подтверждение, где физически расположен DC/DC относительно MCU/кварцев и разъёмов.

---

## 5.5 DFM/DFA: технологичность (допуски, полярности, маркировка, сборочные требования)

ℹ️ Что видно по BOM:
- много 0603/0805/1206 пассивов,
- несколько QFP/TSSOP,
- BGA (ATSAM3X8E LFBGA-144),
- два RJ45, USB-B, microSD, клеммник.

⚠️ Критичные DFM-точки, которые нужно зафиксировать в ПЗ на плату:
1) **BGA (U1):** требования к монтажу (паста/профиль/рентген-контроль) — если это применимо у вашего контрактного производителя.  
2) **microSD holder:** чистота пайки, контроль механики, прижим/прочность.  
3) **RJ45/USB/клеммник:** механическая прочность пайки и фиксации.  
4) Полярности: диоды D*, TVS*, электролиты (если есть), батарея.

⚠️ Чтобы заполнить 5.5 фактами, нужны:
- Assembly drawing (Top/Bottom) и/или pick&place,
- требования производителя (минимальные дорожки/зазоры/маска/паста),
- заметки по “сложным” позициям (если вы уже сталкивались на производстве).

---

## 5.6 Тестируемость (DFT): тест-точки, контрольные измерения, минимальный набор проверок

ℹ️ На уровне “платы” DFT обычно фиксирует:
- что и где мерить,
- какие минимальные проверки обязательны после сборки,
- какие точки доступны без спецоснастки.

⚠️ По текущим файлам я не вижу таблицы test-point’ов и “контрольных напряжений” для платы.
Чтобы сделать 5.6 корректно, нужно одно из двух:
1) PCB top/bottom с подписанными TP, или  
2) отдельная таблица “TP → сигнал → номинал”.

**Минимальный набор контрольных измерений (что точно потребуется):**
- VIN после входной защиты (если точка доступна),
- +5V,
- +3V3,
- (опционально) VBAT/backup домен RTC,
- признаки жизни: Reset, JTAG доступность, базовая активность интерфейсов (на уровне линий).

⚠️ Нужна ваша фиксация: есть ли на плате выделенные TP, или измерения делаются “по ногам разъёмов/конденсаторам”.

---

## 5.7 Перечень и назначение производственных файлов (Gerber/ODB++, drill, pick&place, assembly drawing)

⚠️ Сейчас в загруженных файлах **нет** производственного пакета (gerber/odb/drill/pnp/assy).  
Поэтому 5.7 пока может быть только шаблоном “что должно входить”, без конкретных имён.

**Что обычно входит (as-built, перечень):**
- Gerber (или ODB++): медь/маска/шелк/контур
- NC Drill / Excellon
- Pick&Place (центроиды)
- BOM (у нас есть `BOM LCP.csv`)
- Assembly drawing (Top/Bottom)
- Stackup (у нас есть скрин; лучше экспорт)
- Readme для производства: версии, допуски, покрытия, особые требования.

⚠️ Чтобы заполнить 5.7 фактами, нужно:
- архив “Manufacturing Output” или список файлов релиза (имена/версии).

---

# Следующий шаг по оглавлению

## 6. Элементная база и BOM-логика
(выбор, критичные позиции, замены, EOL-риски)

---

## 6. Элементная база и BOM-логика (выбор, критичные позиции, замены, EOL-риски) — fact/BOM-check

ℹ️ Источники: `BOM LCP.csv`, `LCP schematics.pdf`.  
ℹ️ Цель раздела: зафиксировать **что именно стоит на плате (as-built)**, какие позиции являются **критичными для совместимости/ремонта**, и по каким правилам допустимы замены **без изменения схемы/PCB**.  
⚠️ Здесь **не описываем** доступность на рынке/цены/логистику как анализ; только фиксируем “критичность” и параметры эквивалентности.

---

### 6.1 Принципы выбора компонентов (как зафиксировано в конструкции)

ℹ️ По составу BOM видно, что конструкция опирается на:
- стандартные корпуса пассивов: 0603 / 0805 / 1206 (R/C), ферриты 0805,
- цифровые ИС в LQFP/TSSOP/SOIC/SOT-23,
- МК в BGA (LFBGA-144),
- силовой DC/DC в SOIC-8 с внешним дросселем.

ℹ️ Общие “as-built” принципы, которые следуют из схемы/подбора:
- питание: +24V → импульсный DC/DC → +5V → LDO → +3V3,
- разделение доменов питания МК ферритовыми бусинами (100 MHz) и локальными конденсаторами,
- интерфейсная часть (RS-485/Ethernet/SD) выполнена дискретными специализированными ИС,
- защита интерфейсных линий выполнена TVS-диодами (в BOM присутствует группа `TVS1…`).

⚠️ Вопрос (если хотите оформить “принципы” 6.1 более предметно):
Нужно ли фиксировать целевые классы компонентов по напряжению/температуре (например, “пассива 50V, X7R”), или оставляем только факт “корпуса/номиналы по BOM”?

---

### 6.2 Критичные позиции (ядро платы) и допустимость замен

ℹ️ Ниже — перечень позиций, которые определяют совместимость платы на уровне схемы/PCB.

#### 6.2.1 Микроконтроллер (основная вычислительная ИС)

- U1: **ATSAM3X8EA-AU**, корпус **LFBGA-144** (BOM)

⚠️ Критичность:
- определяет распиновку, питание доменов, тактирование и всю топологию платы вокруг него.

⚠️ Правило замены (as-built):
- прямая замена возможна только на **полный pin-to-pin эквивалент** в том же корпусе (на практике обычно отсутствует).
- любые “похожие” МК = новая аппаратная платформа (это уже не “параметрическая замена”).

---

#### 6.2.2 Ethernet-контроллеры и разъёмы

- U11: **W5500**, корпус **LQFP-48** (BOM)  
- U16: **W5500**, корпус **LQFP-48** (BOM)  
- ETH1/ETH2: `J0011D21BNL`, footprint **RJ45** (BOM)

⚠️ Критичность:
- W5500 задаёт аппаратную архитектуру Ethernet-части (SPI-линии, служебные сигналы, питание 3.3V).
- RJ45 посадочное место/механика — критична для сборки/корпуса и для трассировки.

⚠️ Правило замены:
- W5500: только **тот же чип** или прямой функционально- и pin-to-pin эквивалент (если существует).
- RJ45: замена возможна только при совпадении **посадочного места (footprint)** и механики (высота/крепёж/экранирование).

---

#### 6.2.3 RS-485 трансиверы

- U4…U10: footprint `MAX485 - duplicate`, количество **7 шт.** (BOM)

⚠️ Критичность:
- определяют физический уровень RS-485, уровни питания (+5V), распиновку линий A/B, DE/RE/DI/RO.

⚠️ Правило замены:
- допускается замена на функциональный эквивалент **RS-485 half-duplex, 5V**, при условии:
  - совпадение корпуса/распиновки (pin-to-pin),
  - совместимые уровни логики DE/RE,
  - совместимые требования по скорости/ESD (если заявлены).

⚠️ Вопрос (чтобы описать точно “as-built”):
В BOM для U4…U10 Comment пустой — нужно подтвердить, что реально установлен **MAX485** (как вы ранее говорили), или иной совместимый трансивер.

---

#### 6.2.4 Питание (DC/DC + LDO)

- U2: **TPS54331**, корпус **SOIC-8** (BOM) — buck 24V→5V  
- U3: footprint **SOT-223**, семейство 1117 (BOM: Comment пустой) — LDO 5V→3.3V

⚠️ Критичность:
- U2 задаёт стабильность +5V и поведение запуска/UVLO.
- U3 задаёт +3V3 для МК/логики.

⚠️ Правило замены:
- U2: только pin-to-pin совместимая серия/аналог с тем же режимом (SOIC-8, совместимая частота/компенсация/EN-порог) — иначе потребуется пересчёт/переразводка силовой части.
- U3: допускается замена на LDO 3.3V в SOT-223 с совместимой распиновкой (IN/GND/OUT), при условии:
  - достаточный ток,
  - устойчивость с применёнными выходными конденсаторами (как “as-built”),
  - совместимый dropout и тепловой режим.

⚠️ Вопрос:
Уточнить “as-built” MPN U3 (в схеме подписан как 3.3V, а в BOM Comment пустой).

---

#### 6.2.5 Тактирование (кварцы) и “требования эквивалентности”

- XT1: **32.768 kHz**, footprint `KX – 327` (BOM)  
- XTL6: **12.0 MHz**, footprint `KX-7 XTAL` (BOM)  
- Дополнительно в BOM присутствуют:  
  - XTL2: **14.7456 MHz** (BOM)  
  - XTL3: **16 MHz** (BOM)

⚠️ Критичность:
- кварцы определяют старт/тайминги МК и отдельных интерфейсных подсистем (если задействованы).

⚠️ Правило замены:
- частота должна совпадать 1:1,
- нагрузочная ёмкость (CL) и ESR — в пределах, совместимых с обвязкой,
- корпус/посадочное место должно совпадать.

⚠️ Вопрос:
Какие из XTL2/XTL3 реально “рабочие” в этой плате (на каких листах схемы они стоят и к каким ИС относятся)? Сейчас по BOM они есть, но без контекста листа лучше уточнить.

---

#### 6.2.6 Разъёмы/механика (критичны для корпуса и производства)

- P3 (X2X): `EMSTBA 2.5/5-G-5.08` (BOM)  
- J1: USB-B (BOM: footprint `USB B`)  
- SD1: `DM3AT-SF-PEJM5` microSD holder  
- B1: Battery Holder CR2032  
- P1: Header 6×2 (JTAG) — (BOM: `Header 6X2`)

⚠️ Правило замены:
- только при совпадении footprint + ключевой механики (высота, опоры, крепёж/экран).

---

### 6.3 Ограничения по жизненному циклу (EOL-риск) и стратегия аналогов — “что имеет смысл держать под контролем”

ℹ️ В рамках ПЗ на плату корректно фиксировать **не рыночные оценки**, а “класс позиций, где аналог подбирать сложно”.

⚠️ Повышенная критичность по аналогам (обычно требует заранее согласованных замен):
- U1 (МК в BGA)
- U11/U16 (W5500)
- U2 (DC/DC)
- U4–U10 (RS-485 трансиверы)
- Разъёмы ETH/SD/USB/X2X (из-за механики/footprint)

ℹ️ Практическая стратегия “as-built” (без привязки к поставщикам):
- для критичных ИС держать список **approved substitutions** (см. 6.4),
- для разъёмов — фиксировать **один основной** и 1–2 эквивалента только при 100% совпадении посадки.

⚠️ Вопрос:
Вы хотите, чтобы в этой ПЗ был отдельный **список “разрешённых замен”** (approved substitutions) по факту вашей практики, или мы оставляем только правила эквивалентности (6.4)?

---

### 6.4 Правила параметрических замен (что считать эквивалентом)

ℹ️ Этот пункт задаёт “рамку” для инженера сопровождения: что можно менять без изменения схемы/PCB.

#### 6.4.1 Пассивы (R/C/L, ферриты)

- **Резисторы:** номинал, допуск, мощность, корпус.  
- **Конденсаторы:** ёмкость, напряжение, диэлектрик (X7R/X5R), корпус.  
- **Ферриты:** импеданс @100MHz (как в BOM `100MHz`), ток, DCR, корпус 0805.

⚠️ Для цепей питания/такта/высоких фронтов (SW, кварцы, USB, SPI) пассивы считать “критичными” — менять только с пониманием цепи и сохранением параметров.

#### 6.4.2 Диоды/TVS

- **Schottky (D1/D2):** VRRM, IF, VF, корпус (SMA/DO-214), тепловой режим.  
- **TVS:** рабочее напряжение, тип (uni/bi), импульсная мощность, корпус DO-214.

⚠️ У вас на плате группа TVS с Comment `SMAJ5.0CA` — это фактически “семейство” допустимых замен при сохранении параметров.

#### 6.4.3 ИС питания (buck/LDO)

- Совпадение корпуса и распиновки (pin-to-pin) — обязательное.  
- Совместимость с применёнными L/C (устойчивость, компенсация).  
- Диапазон входа, ток, EN-порог/UVLO.

#### 6.4.4 Интерфейсные ИС (RS-485, Ethernet, UART expander)

- **RS-485:** pin-to-pin, питание 5V, логические уровни DE/RE, ESD.  
- **Ethernet (W5500):** практически только прямой эквивалент (если он существует) при совпадении SPI/служебных сигналов.  
- **SC16IS752:** замена только на совместимую серию/аналог при совпадении интерфейса к МК и распиновки.

---

## Вопросы/уточнения по разделу 6 (чтобы “закрыть” без пробелов)

1) Подтвердить “as-built” для U4–U10: какой именно трансивер стоит (MAX485 или эквивалент).  
2) Уточнить MPN U3 (LDO 3.3V в SOT-223).  
3) По кварцам XTL2 (14.7456MHz) и XTL3 (16MHz): к каким узлам они относятся и используются ли в данной ревизии.  
4) Есть ли у вас уже согласованный список **approved substitutions** (можно простым списком: RefDes → допустимые MPN).

---

# Следующий пункт по оглавлению

## 7. Встроенные функции, влияющие на аппаратную часть
⚠️ Напоминание по scope: в 7.* оставляем только то, что **про железо** (boot-страпы, линии reset/erase, аппаратные таймеры/WDG как выводы/цепи), без описания логики прошивки.



---

## 7. Встроенные функции, влияющие на аппаратную часть — fact/BOM-check

ℹ️ Scope: в 7.* фиксируем **только аппаратные цепи/линии**, которые обеспечивают загрузку/восстановление/сервис и аппаратно-зависимые условия (кварцы, reset, JTAG, backup-питание, strap-пины).  
⚠️ Не описываем: алгоритмы прошивки, протоколы, сценарии эксплуатации изделия.

---

### 7.1 Режимы загрузки/обновления: аппаратные линии boot/recovery (as-built)

#### 7.1.1 JTAG (программирование/отладка на плате)

ℹ️ На плате предусмотрен сервисный интерфейс JTAG, выведенный на разъём.

**RefDes/MPN (по BOM):**
- P1: `Header 6X2` (разъём JTAG)

⚠️ Важно для сопровождения:
- наличие JTAG разъёма задаёт “точку восстановления” в случае проблем с прошивкой/памятью;
- распиновка P1 должна быть зафиксирована 1:1 (пины TCK/TMS/TDI/TDO/NRST/VTref/GND).

⚠️ Вопрос:
Нужен pinout P1 “как в Altium” (пин 1…12 → сигнал). По текущему набору файлов это не видно однозначно.

---

#### 7.1.2 Аппаратный Reset (MASTER-RESET)

ℹ️ На схеме процессора присутствует линия **`MASTER-RESET`** с RC-обвязкой и кнопкой.

**RefDes (по Processor.SchDoc):**
- Btn1 — кнопка (используется в reset-цепи)
- C26 = 6.8 nF (в цепи `MASTER-RESET`)

⚠️ Замечание по “железу”:
- внешнего отдельного супервизора/reset-IC на схеме процессора не видно (reset формируется линией + RC + внутренними механизмами МК).

⚠️ Вопрос:
Есть ли на других листах схемы отдельная микросхема supervisor/reset? Если нет — фиксируем как “as-built: reset без внешнего supervisor”.

---

#### 7.1.3 Strap/служебные пины МК (TST / ERASE / Recovery)

ℹ️ На листе процессора видно использование служебного пина **`TST`** и отдельной сервисной кнопки **Btn2**.

**RefDes (по Processor.SchDoc/BOM):**
- R1 = 100 kΩ (подключён к линии `TST` — подтяжка)
- Btn2 — тактовая кнопка (отдельно от reset)

⚠️ На схеме рядом с верхними пинами МК есть пометка **“N.C. internal pullup”** — часть пинов оставлена не подключённой и полагается на внутренние подтяжки МК (это важно не “улучшать” без понимания даташита).

⚠️ Вопросы (чтобы корректно описать без домыслов):
1) Btn2 — это **ERASE/BOOT/Recovery** или пользовательская кнопка? На фрагменте видно подключение к `+3V3`, но точное имя net/пина нужно подтвердить.  
2) Какие конкретно пины МК задействованы для recovery (ERASE, TST, и т.п.) и как они выведены (только кнопка/только JTAG/есть ли тест-пады)?

---

### 7.2 Watchdog / реакция на сбои питания: аппаратная часть (as-built)

#### 7.2.1 Watchdog

ℹ️ Для ATSAM3X8E watchdog является внутренней функцией МК.  
ℹ️ На уровне схемы **внешняя микросхема watchdog** не прослеживается (в текущем наборе листов).

⚠️ Вопрос:
Подтвердить, что на плате **нет отдельного** watchdog/supervisor IC (если он есть — укажи RefDes/лист).

---

#### 7.2.2 Поведение при сбоях питания (аппаратный слой)

ℹ️ Аппаратная часть питания и reset-цепь задают “физические” условия старта:
- питание МК от `+3V3` (через U3),
- reset линия `MASTER-RESET`,
- кварцы (12 MHz и 32.768 kHz) как условия корректного старта тактирования (см. 4.3).

⚠️ Отдельных аппаратных “brown-out” цепей на плате не видно; если защита от просадки реализована, то она относится к внутренним функциям МК (и остаётся за рамками ПЗ платы как схемного узла).

---

#### 7.2.3 Backup-питание RTC/backup домена

ℹ️ На плате реализовано питание backup-домена от батарейки CR2032 через диодную развязку.

**RefDes/MPN (по BOM/Processor.SchDoc):**
- B1: Battery Holder CR2032
- D4: `BAT54C` (диодная сборка, “диод OR” между `+3V3` и VBAT)

ℹ️ Дополнительно присутствует аппаратный узел контроля батареи:
- U21: `TS391ILT`
- R2/R102/R103: 10 kΩ (делитель/формирование порога)
- выход уходит на вход МК (на схеме отмечен как `PIN50`)

⚠️ Вопрос:
Нужно подтвердить назначение U21: “battery low” / “battery present” / иной порог и какая логика на вход МК (активный уровень).

---

### 7.3 Аппаратно-зависимые параметры (то, что прошивка обязана учитывать, но описываем только железо)

ℹ️ Здесь фиксируем “аппаратный контракт”: какие частоты, уровни и сигнальные линии заданы схемой и не должны меняться без пересмотра железа.

#### 7.3.1 Тактирование и частоты (as-built)

- XTL6: 12.0 MHz + C17/C18 = 18 pF (основной кварц МК)
- XT1: 32.768 kHz + C19/C20 = 18 pF (RTC кварц)

⚠️ Инвариант:
- частоты кварцев и их обвязка являются частью аппаратной платформы (при замене кварца важно сохранить частоту/CL/корпус).

---

#### 7.3.2 Домены питания и их разделение (as-built)

ℹ️ На уровне схемы фиксируются домены:
- `+5V` (buck U2) — питание части интерфейсных драйверов/индикации (по вашей вводной)
- `+3V3` (LDO U3) — питание МК и 3.3V логики
- разделение доменов питания МК ферритами L1–L4 (100 MHz) и локальными конденсаторами

⚠️ Инвариант:
- точки разделения (ферриты/развязка) и локальная ёмкость должны сохраняться при модификациях.

---

#### 7.3.3 Аппаратные линии “chip select / interrupt / detect” (as-built)

ℹ️ На листе процессора присутствуют аппаратные сигналы, которые “жёстко” закрепляют распиновку:
- SPI линии (MOSI/MISO/SPCK)
- CS-линии для устройств (`CS_SD`, `CS_ETH_1`, `CS_ETH_2`, `CS_UA1/2/3` и т.п. — по надписям на Processor.SchDoc)
- линии прерываний/событий (`INT_CAN`, `INTN_ETH`, и т.п.)
- `SD_Detect` (детект microSD)

⚠️ Инвариант:
- эти сети и их назначение должны сохраняться между ревизиями платы (это “аппаратный контракт” для ПО, но в ПЗ фиксируем только факт наличия/распиновки).

⚠️ Вопрос:
Нужна выгрузка/таблица “Net → пин МК” для основных CS/INT/Detect (её можно получить из Altium). По PDF часть подписей читается, но лучше зафиксировать списком в приложении.

---

## Вопросы/уточнения по разделу 7 (чтобы закрыть без пробелов)

1) Btn2 — это **ERASE/Recovery** или пользовательская кнопка? Нужен точный net/пин.  
2) Есть ли на плате отдельный supervisor/reset/watchdog IC (если да — RefDes/лист).  
3) Нужен pinout разъёма P1 (JTAG) в явном виде (пин 1..12).  
4) Уточнить назначение U21 (TS391) и активный уровень сигнала на вход МК.

---


8. Интерфейсы и разъёмы (в этой ПЗ — только “карта разъёмов и физика”)
8.1 Карта разъёмов (pinout / уровни / назначение)
P1 — силовой/линейный разъём (питание + линия X2X)

По листу Plugs.SchDoc разъём обозначен как P1: MCD 1.5/6-G1-3.81. На него выведены сети:

+24V

X_H

X_L

а также несколько контактов GND (A1…A6 — земли/общие контакты на листе разъёмов).

LCP schematics

ℹ️ Примечание.
По схеме “X_H / X_L” — это отдельная дифф./линейная пара (логический смысл — вне scope этой ПЗ, здесь фиксируем только физическое наличие и имена сетей).

LCP schematics

⚠️ Открытый вопрос (для закрытия pinout-таблицы P1).
Нужна точная нумерация контактов P1 → сетей (Pin1/Pin2/…): в текущем текстовом извлечении PDF видны только имена сетей, без явной привязки к номерам пинов.

J1 — USB (служебный порт “Firmware/Debug”)

На листе Plugs.SchDoc USB подписан как USB - Firmware/Debug, разъём J1 (XUSB). Фиксируем pinout, как он указан на листе:

VUSB = 1

D- = 2

D+ = 3

GND = 4

UGND = 0

LCP schematics

На этом же листе сети для USB-линий обозначены как:

D-_N

D+_P

GND

LCP schematics

⚠️ Открытый вопрос (уровни/питание USB).
Нужно уточнить: VUSB используется только как “VBUS detect”/питание интерфейса или реально питает часть схемы (и есть ли ограничение/развязка по питанию USB).

P3 — X2X (шина/межблочный разъём)

На листе разъёмов присутствует позиция P3 (X2X).

LCP schematics

⚠️ Открытый вопрос (pinout P3).
В текстовом извлечении PDF не видна распиновка P3 (Pin1…Pin5 → сети).
С твоих слов ранее: 5 контактов, где 2 контакта +24V, 1 контакт NC, 2 контакта RS-485 A/B. Чтобы это зафиксировать корректно “1:1 со схемой”, нужно подтвердить точное соответствие “номер контакта → сеть/назначение”.

RS-485 линии (имена дифф. пар на плате)

На листе разъёмов перечислены дифференциальные пары RS-485:

RS1_N / RS1_P

RS2_N / RS2_P

RS3_N / RS3_P

RS4_N / RS4_P

RS5_N / RS5_P

RS6_N / RS6_P

LCP schematics

ℹ️ Примечание.
В этой ПЗ фиксируем именно физический уровень: наличие пар, имена сетей, и куда они уходят (в разъёмы/клеммы). Логика протокола и “кто с кем общается” — вне scope.

Ethernet

На листе разъёмов присутствует секция Ethernet (on Eth Page) — детали вынесены на отдельный лист схемы Ethernet.

LCP schematics

⚠️ Открытый вопрос (закрыть по PDF).
Нужно из “Eth Page” вытащить:

тип разъёма (RJ45 / с магнитикой или без),

сети TX/RX-пар,

ESD/защитные элементы (если есть),

уровни питания PHY/контроллера и служебные сигналы (INT/RST/CS — если применимо).

microSD

На листе разъёмов присутствует секция microSD Holder (on SD Page) — детали вынесены на отдельный лист SD.

LCP schematics

⚠️ Открытый вопрос (закрыть по PDF).
Нужно из “SD Page” вытащить:

тип/модель держателя,

линии интерфейса (CMD/CLK/DAT0…),

CS/Detect/WriteProtect (если есть),

питание карты и уровни логики.

8.2 Уровни сигналов и электрическая совместимость (только физика)
RS-485

По листу RS485.SchDoc применены трансиверы SN65HVD1785D (несколько экземпляров: U5/U7/U9/U6/U8/U10 и др. по листу).

LCP schematics

LCP schematics

Типовые сигналы управления/данных, заведённые на трансивер (по подписи выводов на фрагменте):

Rx, Tx

TxEn1, TxEn2

шина: A, B

питание: +5V, общий: GND

LCP schematics

LCP schematics

ℹ️ Примечание.
Факт использования +5V для питания RS-485-трансиверов фиксируем как “as-built”; причины/расчёты (помехоустойчивость/запас по уровню) — отдельным пунктом в 4.5/6.* при необходимости.

⚠️ Открытые вопросы (для закрытия физики RS-485 полностью):

Где именно (в каких разъёмах/клеммах) сидят пары RS1…RS6 — нужна привязка “пара → разъём → пины”.

Есть ли на каждой линии A/B защита (TVS), последовательные резисторы/RC, и как она включена.

Есть ли оконечивание/смещение (termination/bias) и где оно реализовано.

USB

По листу разъёмов USB присутствуют стандартные линии D+/D-, а также VUSB и земли GND/UGND.

LCP schematics


⚠️ Открытый вопрос: есть ли на D+/D- ESD-защита и как реализовано согласование (если оно есть) — нужен лист, где стоит USB-обвязка.

Ethernet / microSD

⚠️ Пока не заполнено: данные вынесены на отдельные страницы (“Eth Page”, “SD Page”), их нужно извлечь из PDF.

8.3 Защита линий на разъёмах (ESD/перенапряжения) — только “что стоит на схеме”
RS-485

В текущем видимом фрагменте RS485.SchDoc явно читаются трансиверы и часть пассивов, но защитные цепи на A/B в этом куске не идентифицированы однозначно (нужен полный лист/страница RS-485).

LCP schematics

⚠️ Открытый вопрос.
Подтверди, пожалуйста: на RS-линиях стоят TVS-диоды (и какие именно RefDes), или защита реализована только на уровне трансивера/топологии.

USB / Ethernet / microSD

⚠️ Пока не заполнено: требуется “Eth Page” и “SD Page”, а также лист, где разведён USB (если он не только на “Plugs”).


























---



### Правило хранения данных (строгий регламент)

#### Библиотека компонента (SchLib / PcbLib)
| Тип компонента | Default Designator | Default Comment | Description | User Params (Mfr/MPN/…) | Footprint model |
|---|---:|---:|---:|---:|---:|
| Пассивки (R/C/L/простые диоды) | ✅ | ❌ | ❌ | ❌ | ✅ |
| Уникальные ИС/MCU | ✅ | ❌ | ❌ | ❌ | ✅ |
| Разъёмы/клеммы | ✅ | ❌ | ❌ | ❌ | ✅ |
| Питание/модули/реле | ✅ | ❌ | ❌ | ❌ | ✅ |

#### Данные в проекте (SchDoc)
| Тип компонента | Comment | Description | Manufacturer | Manufacturer Part Number | BOM Description |
|---|---:|---:|---:|---:|---:|
| Пассивки (R/C/L/простые диоды) | ✅ | ❌ | ✅ | ✅ | ✅ |
| Уникальные ИС/MCU | ❌ | ❌ | ✅ | ✅ | ✅ |
| Разъёмы/клеммы | ❌ | ❌ | ✅ | ✅ | ✅ |
| Питание/модули/реле | ❌ | ❌ | ✅ | ✅ | ✅ |



## Примеры заполнения (эталоны)

### Пример 1 — Резистор (пассивка)
#### Библиотека компонента (SchLib / PcbLib)
| Поле | Значение |
|---|---|
| Default Designator | `R?` |
| Default Comment | *(пусто)* |
| Description | *(пусто)* |
| User Params (Mfr/MPN/…) | *(пусто)* |
| Footprint model | `RES_0603` *(пример)* |

#### Данные в проекте (SchDoc)
| Поле | Значение |
|---|---|
| Designator | `R24` *(пример)* |
| Comment | `240R` *(пример)* |
| Description | *(пусто)* |
| Manufacturer | `Yageo` *(пример)* |
| Manufacturer Part Number | `RC0603FR-07240RL` *(пример)* |
| BOM Description | `Resistor 240R 1% 0.1W 0603` *(пример)* |

---

### Пример 2 — МК SAM3X8E (уникальная ИС)
#### Библиотека компонента (SchLib / PcbLib)
| Поле | Значение |
|---|---|
| Default Designator | `U?` |
| Default Comment | *(пусто)* |
| Description | *(пусто)* |
| User Params (Mfr/MPN/…) | *(пусто)* |
| Footprint model | `LQFP144_20x20_0.5mm` *(пример)* |

#### Данные в проекте (SchDoc)
| Поле | Значение |
|---|---|
| Designator | `U1` *(пример)* |
| Comment | *(пусто)* |
| Description | *(пусто)* |
| Manufacturer | `Microchip` *(пример)* |
| Manufacturer Part Number | `ATSAM3X8EA-AU` *(пример)* |
| BOM Description | `MCU ATSAM3X8E, ARM Cortex-M3, LQFP144` *(пример)* |
