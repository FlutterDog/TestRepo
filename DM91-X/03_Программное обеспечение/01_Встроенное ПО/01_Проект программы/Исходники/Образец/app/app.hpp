#pragma once

#include "project.hpp"

/**
 * @file app.hpp
 * @brief Публичный интерфейс прикладного уровня LAI.
 *
 * @details
 * Файл объявляет функции жизненного цикла прикладной программы:
 * - setup() — однократная инициализация после старта микроконтроллера;
 * - loop() — основной циклический обработчик.
 *
 * Реализация прикладной логики находится в app.cpp.
 */

/**
 * @brief Выполняет однократную инициализацию приложения.
 *
 * @details
 * Функция вызывается один раз после старта микроконтроллера.
 * Внутри выполняется настройка аппаратного слоя платы, интерфейсов обмена,
 * прикладных параметров, Modbus RTU и начальных состояний модуля.
 */
void setup(void);

/**
 * @brief Выполняет основной циклический обработчик приложения.
 *
 * @details
 * Функция вызывается непрерывно из главного цикла программы.
 * Внутри обслуживаются Modbus RTU, аналоговые входы, диагностика,
 * индикация, сохранение параметров и отложенные сервисные операции.
 */
void loop(void);