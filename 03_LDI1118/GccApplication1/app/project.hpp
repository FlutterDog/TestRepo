#pragma once

/**
 * @file project.hpp
 * @brief Общие настройки сборки проекта LDO.
 *
 * @details
 * Файл задаёт базовые параметры проекта и подключает общий аппаратный слой.
 *
 * Здесь должны находиться только настройки, общие для всей прошивки:
 * - частота тактирования микроконтроллера;
 * - базовые системные типы;
 * - подключение общего HAL.
 *
 * Файл не должен содержать:
 * - прикладную логику;
 * - карту Modbus-регистров;
 * - EEPROM-карту;
 * - глобальные переменные приложения;
 * - карту выводов конкретной ревизии платы.
 *
 * Аппаратная карта LDO находится в ldo_board.hpp / ldo_board.cpp.
 */

#ifndef F_CPU
#  define F_CPU 16000000UL
#endif

#include <stdint.h>
#include <stdbool.h>

#include "../hal/compat.hpp"